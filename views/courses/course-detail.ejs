<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= course.title %> | AstroDataHub</title>
    <link rel="stylesheet" href="/css/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
</head>
<body style="background-color: var(--bg-body);">

    <%- include('../partials/header') %>

    <main class="container" style="padding: 4rem 20px;">
        
        <nav style="margin-bottom: 2rem;">
            <a href="/courses" style="font-size: 0.85rem; color: var(--primary); font-weight: 600; text-decoration: none;">← Back to Catalog</a>
        </nav>

        <div style="display: grid; grid-template-columns: 2fr 1fr; gap: 3rem; align-items: start;">
            
            <div>
                <header style="margin-bottom: 3rem;">
                    <div style="display: flex; gap: 10px; margin-bottom: 1rem;">
                        <span style="background: #eff6ff; color: var(--primary); padding: 4px 12px; border-radius: 4px; font-size: 0.75rem; font-weight: 700; text-transform: uppercase;">
                            <%= course.subject %>
                        </span>
                        <span style="background: #f8fafc; border: 1px solid var(--border); color: var(--text-muted); padding: 4px 12px; border-radius: 4px; font-size: 0.75rem; font-weight: 600;">
                            Level: <%= course.level %>
                        </span>
                    </div>
                    <h1 style="font-size: 2.5rem; line-height: 1.2; margin-bottom: 1.5rem;"><%= course.title %></h1>
                    
                    <div style="font-size: 1.1rem; color: var(--text-muted); line-height: 1.7;">
                        <%- course.description %>
                    </div>
                </header>

                <section>
                    <h2 style="font-size: 1.5rem; margin-bottom: 2rem; border-bottom: 2px solid var(--border); padding-bottom: 0.75rem;">Curriculum Modules</h2>
                    
                    <% if (modules && modules.length > 0) { %>
                        <div style="display: flex; flex-direction: column; gap: 1rem;">
                            <% modules.forEach((module, index) => { %>
                                <div class="auth-card" style="margin-bottom: 0; padding: 1.25rem 1.5rem; display: flex; align-items: center; justify-content: space-between; border-radius: 10px;">
                                    <div style="display: flex; align-items: center; gap: 1.5rem;">
                                        <span style="font-size: 1.25rem; font-weight: 800; color: #e2e8f0;"><%= String(index + 1).padStart(2, '0') %></span>
                                        <div>
                                            <h4 style="margin: 0; font-size: 1rem; color: var(--text-main);"><%= module.module_title %></h4>
                                            <span style="font-size: 0.75rem; color: var(--text-muted);">Research Content Unit</span>
                                        </div>
                                    </div>
                                    
                                    <div style="display: flex; align-items: center; gap: 1rem;">
                                        <% if (user && isEnrolled) { %>
                                            <% if (module.quiz) { %>
                                                <a href="/courses/quiz/<%= module.quiz.id %>" 
                                                   style="font-size: 0.75rem; font-weight: 700; background: var(--primary); color: white; padding: 6px 12px; border-radius: 6px; text-decoration: none; text-transform: uppercase; letter-spacing: 0.5px;">
                                                    Take Quiz
                                                </a>
                                            <% } %>

                                            <a href="/courses/course/<%= course.id %>/module/<%= module.id %>" style="font-size: 0.85rem; font-weight: 600; color: var(--primary); text-decoration: none;">View Module →</a>
                                        <% } else if (user && !isEnrolled) { %>
                                            <span style="font-size: 0.75rem; color: var(--text-muted); font-style: italic;">Enroll to Unlock</span>
                                        <% } %>
                                    </div>
                                </div>
                            <% }) %>
                        </div>
                    <% } else { %>
                        <div style="padding: 2rem; background: #fff; border: 1px dashed var(--border); border-radius: 12px; text-align: center;">
                            <p style="color: var(--text-muted);">Modules for this mission are currently being encrypted. Check back soon.</p>
                        </div>
                    <% } %>
                </section>
            </div>

            <aside>
                <div class="auth-card" style="position: sticky; top: 100px; padding: 2rem; border-radius: 16px;">
                    <h4 style="margin-bottom: 1rem;">Course Status</h4>
                    <p style="font-size: 0.85rem; color: var(--text-muted); margin-bottom: 1.5rem;">
                        Enroll to gain full access to datasets, research modules, and certification of completion.
                    </p>
                    
                    <div style="margin-bottom: 2rem;">
                        <div style="display: flex; justify-content: space-between; font-size: 0.9rem; margin-bottom: 0.5rem;">
                            <span style="color: var(--text-muted);">Duration:</span>
                            <span style="font-weight: 600;">Self-paced</span>
                        </div>
                        <div style="display: flex; justify-content: space-between; font-size: 0.9rem;">
                            <span style="color: var(--text-muted);">Format:</span>
                            <span style="font-weight: 600;">Digital Terminal</span>
                        </div>
                    </div>

                    <% if (user) { %>
                        <% if (isEnrolled) { %>
                            <div style="text-align: center;">
                                <div style="background: #f0fdf4; color: #166534; padding: 10px; border-radius: 8px; margin-bottom: 1rem; font-size: 0.85rem; font-weight: 600;">
                                    ✓ Mission Active
                                </div>
                                <a href="/" class="btn-primary" style="display: block; text-align: center; text-decoration: none; background: #1e293b; margin-bottom: 2rem;">Back to Dashboard</a>
                                
                                <div style="border-top: 1px solid var(--border); padding-top: 1.5rem; text-align: left;">
                                    <h5 style="color: #dc2626; font-size: 0.8rem; margin-bottom: 0.5rem; text-transform: uppercase; letter-spacing: 0.5px;">Danger Zone</h5>
                                    <form action="/courses/course/<%= course.id %>/unenroll" method="POST" onsubmit="return confirm('Are you sure you want to unenroll? This will remove this course from your dashboard.');">
                                        <button type="submit" style="background: transparent; color: #b91c1c; border: 1px solid #fecaca; width: 100%; padding: 8px; border-radius: 6px; font-size: 0.75rem; font-weight: 600; cursor: pointer; transition: all 0.2s;">
                                            Unenroll from Course
                                        </button>
                                    </form>
                                </div>
                            </div>
                        <% } else { %>
                            <form action="/courses/enroll/<%= course.id %>" method="POST">
                                <button type="submit" class="btn-primary" style="width: 100%;">Enroll in Course</button>
                            </form>
                        <% } %>
                    <% } else { %>
                        <a href="/auth/login" class="btn-primary" style="display: block; text-align: center; text-decoration: none;">Sign In to Enroll</a>
                    <% } %>

                    <div style="margin-top: 1.5rem; text-align: center;">
                        <span style="font-size: 0.75rem; color: var(--text-muted); font-weight: 500;">Secure Academic Uplink Guaranteed</span>
                    </div>
                </div>
            </aside>

        </div>
    </main>

    <%- include('../partials/footer') %>

</body>
</html>