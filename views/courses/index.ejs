<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> | AstroDataHub</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/css/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;600;700;800&display=swap" rel="stylesheet">
    <style>
        :root { 
            --accent: #2563eb; 
            --text-dark: #0f172a; 
            --text-muted: #64748b;
            --bg-light: #f8fafc;
        }

        body { 
            font-family: 'Plus Jakarta Sans', sans-serif; 
            background-color: var(--bg-light); 
        }

        /* Udemy-style Card Hover */
        .course-card {
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            border: 1px solid #e2e8f0;
            background: white;
        }

        .course-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 20px 25px -5px rgb(0 0 0 / 0.1), 0 8px 10px -6px rgb(0 0 0 / 0.1);
            border-color: var(--accent);
        }

        .sidebar-link {
            display: block;
            padding: 10px 16px;
            border-radius: 12px;
            color: var(--text-muted);
            font-weight: 600;
            transition: all 0.2s;
            border-left: 4px solid transparent;
            font-size: 0.875rem;
        }

        .sidebar-link:hover {
            background: white;
            color: var(--accent);
        }

        /* Active Filter State */
        .sidebar-link.active {
            background: white;
            color: var(--accent);
            box-shadow: 0 4px 6px -1px rgb(0 0 0 / 0.05);
            border-left: 4px solid var(--accent);
        }

        .search-container {
            background: white;
            border: 1px solid #e2e8f0;
            border-radius: 16px;
            padding: 6px;
            display: flex;
            align-items: center;
            box-shadow: 0 4px 6px -1px rgb(0 0 0 / 0.05);
        }

        .custom-scrollbar::-webkit-scrollbar { width: 4px; }
        .custom-scrollbar::-webkit-scrollbar-thumb { background: #e2e8f0; border-radius: 10px; }
    </style>
</head>
<body class="antialiased">
    <%- include('../partials/header') %>

    <section class="bg-white border-b border-slate-200 pt-32 pb-16">
        <div class="max-w-7xl mx-auto px-6">
            <h1 class="text-5xl font-extrabold text-slate-900 tracking-tight mb-4">Educational Cosmos</h1>
            <p class="text-xl text-slate-500 max-w-2xl">A complete learning trajectory from Class 1 foundations to professional PhD research.</p>
            
            <form action="/courses" method="GET" class="search-container mt-10 max-w-lg">
                <input 
                    type="text" 
                    name="search" 
                    placeholder="Search courses, subjects, or levels..." 
                    value="<%= activeSearch %>"
                    class="flex-1 px-4 py-2 outline-none text-slate-600 font-medium"
                >
                <button type="submit" class="bg-blue-600 text-white px-6 py-3 rounded-xl font-bold hover:bg-blue-700 transition-colors">
                    Search
                </button>
            </form>
        </div>
    </section>

    <div class="max-w-7xl mx-auto px-6 lg:px-8 py-16">
        <div class="flex flex-col lg:flex-row gap-12">
            
            <aside class="w-full lg:w-64 flex-shrink-0">
                <div class="sticky top-28">
                    
                    <div class="mb-10">
                        <h3 class="text-xs font-bold text-slate-400 uppercase tracking-widest mb-6">Academic Path</h3>
                        <nav class="space-y-1 max-h-[500px] overflow-y-auto custom-scrollbar pr-2">
                            
                            <div class="px-4 py-2 text-[10px] font-bold text-slate-400 uppercase tracking-wider">Primary & Secondary</div>
                            <% for(let i=1; i<=12; i++) { %>
                                <a href="/courses?standard=Class <%= i %>" class="sidebar-link <%= activeStandard === 'Class ' + i ? 'active' : '' %>">
                                    Class <%= i %>
                                </a>
                            <% } %>

                            <div class="px-4 py-2 mt-4 text-[10px] font-bold text-slate-400 uppercase tracking-wider">Higher Education</div>
                            <% ["Bachelors", "Masters", "PhD / Doctorate"].forEach(std => { %>
                                <a href="/courses?standard=<%= std %>" class="sidebar-link <%= activeStandard === std ? 'active' : '' %>">
                                    <%= std %>
                                </a>
                            <% }) %>

                            <div class="px-4 py-2 mt-4 text-[10px] font-bold text-slate-400 uppercase tracking-wider">Professional</div>
                            <a href="/courses?standard=Research" class="sidebar-link <%= activeStandard === 'Research' ? 'active' : '' %>">
                                Research & Thesis
                            </a>
                        </nav>
                    </div>

                    <div class="mb-10">
                        <h3 class="text-xs font-bold text-slate-400 uppercase tracking-widest mb-6">Difficulty</h3>
                        <nav class="space-y-1">
                            <a href="/courses" class="sidebar-link <%= !activeLevel && !activeSubject && !activeStandard ? 'active' : '' %>">
                                All Courses
                            </a>
                            <a href="/courses?level=Beginner" class="sidebar-link <%= activeLevel === 'Beginner' ? 'active' : '' %>">
                                Beginner
                            </a>
                            <a href="/courses?level=Intermediate" class="sidebar-link <%= activeLevel === 'Intermediate' ? 'active' : '' %>">
                                Intermediate
                            </a>
                            <a href="/courses?level=Expert" class="sidebar-link <%= activeLevel === 'Expert' ? 'active' : '' %>">
                                Expert
                            </a>
                        </nav>
                    </div>

                    <div class="mb-10">
                        <h3 class="text-xs font-bold text-slate-400 uppercase tracking-widest mb-6">Categories</h3>
                        <nav class="space-y-1">
                            <a href="/courses?subject=Mathematics" class="sidebar-link <%= activeSubject === 'Mathematics' ? 'active' : '' %>">
                                Mathematics
                            </a>
                            <a href="/courses?subject=Astronomy" class="sidebar-link <%= activeSubject === 'Astronomy' ? 'active' : '' %>">
                                Astronomy
                            </a>
                            <a href="/courses?subject=Computer Science" class="sidebar-link <%= activeSubject === 'Computer Science' ? 'active' : '' %>">
                                Computer Science
                            </a>
                        </nav>
                    </div>

                    <% if (activeLevel || activeSubject || activeSearch || activeStandard) { %>
                        <div class="mt-8 pt-6 border-t border-slate-200">
                            <a href="/courses" class="text-sm font-bold text-red-500 hover:text-red-700 flex items-center gap-2">
                                ✕ Reset All Filters
                            </a>
                        </div>
                    <% } %>
                </div>
            </aside>

            <main class="flex-1">
                <div class="flex items-center justify-between mb-8">
                    <h2 class="text-2xl font-bold text-slate-900">
                        <% if (activeSearch) { %>
                            Results for "<%= activeSearch %>"
                        <% } else if (activeStandard) { %>
                            <%= activeStandard %> Programs
                        <% } else if (activeSubject || activeLevel) { %>
                            Filtered Results
                        <% } else { %>
                            Recommended Missions
                        <% } %>
                    </h2>
                    <span class="text-sm font-semibold text-slate-500"><%= courses.length %> Courses found</span>
                </div>

                <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
                    <% if (courses && courses.length > 0) { %>
                        <% courses.forEach(course => { %>
                            <div class="course-card rounded-3xl overflow-hidden flex flex-col">
                                <div class="h-48 bg-slate-100 flex items-center justify-center border-b border-slate-100 relative">
                                    <span class="text-5xl">🚀</span>
                                    <% if (course.standard) { %>
                                        <span class="absolute top-4 left-4 bg-blue-600 text-white px-3 py-1 rounded-full text-[10px] font-bold shadow-sm uppercase">
                                            <%= course.standard %>
                                        </span>
                                    <% } %>
                                    <span class="absolute top-4 right-4 bg-white/80 backdrop-blur px-3 py-1 rounded-full text-[10px] font-bold text-slate-600 shadow-sm">
                                        ID: #<%= course.id %>
                                    </span>
                                </div>
                                
                                <div class="p-8 flex-1 flex flex-col">
                                    <div class="flex gap-2 mb-4">
                                        <span class="bg-blue-50 text-blue-600 px-3 py-1 rounded-lg text-[10px] font-bold uppercase tracking-wider">
                                            <%= course.subject %>
                                        </span>
                                        <span class="bg-slate-50 text-slate-500 px-3 py-1 rounded-lg text-[10px] font-bold uppercase tracking-wider">
                                            <%= course.level %>
                                        </span>
                                    </div>
                                    
                                    <h3 class="text-xl font-bold text-slate-900 mb-3 leading-tight hover:text-blue-600 transition-colors">
                                        <%= course.title %>
                                    </h3>
                                    
                                    <div class="text-slate-500 text-sm line-clamp-2 mb-6">
                                        <%= course.description.replace(/<[^>]*>?/gm, '').substring(0, 100) %>...
                                    </div>

                                    <div class="mt-auto pt-6 border-t border-slate-50 flex items-center justify-between">
                                        <div class="flex items-center gap-1">
                                            <span class="text-yellow-400 text-lg">★</span>
                                            <span class="font-bold text-slate-900">4.9</span>
                                        </div>
                                        <a href="/courses/course/<%= course.id %>" class="text-blue-600 font-bold hover:underline">
                                            Explore Details →
                                        </a>
                                    </div>
                                </div>
                            </div>
                        <% }) %>
                    <% } else { %>
                        <div class="col-span-full py-24 bg-white rounded-3xl border-2 border-dashed border-slate-200 text-center">
                            <div class="text-6xl mb-6">🔭</div>
                            <h3 class="text-xl font-bold text-slate-900 mb-2">No missions found</h3>
                            <p class="text-slate-500 mb-8">Try adjusting your filters or search terms.</p>
                            <a href="/courses" class="bg-slate-900 text-white px-8 py-3 rounded-xl font-bold hover:bg-slate-800 transition-all">
                                View All Curriculum
                            </a>
                        </div>
                    <% } %>
                </div>
            </main>
        </div>
    </div>

    <%- include('../partials/footer') %>
</body>
</html>