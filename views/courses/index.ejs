<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> | AstroDataHub</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/css/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <style>
        :root { --accent: #2563eb; --open-green: #10b981; }
        body { font-family: 'Plus Jakarta Sans', sans-serif; background-color: #f8fafc; }

        /* Navigation Glass */
        .sidebar-item { display: flex; align-items: center; justify-content: space-between; padding: 12px 16px; border-radius: 14px; font-size: 0.875rem; color: #475569; transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1); font-weight: 600; }
        .sidebar-item:hover { background: white; color: var(--accent); transform: translateX(4px); }
        .sidebar-item.active { background: var(--accent); color: white; box-shadow: 0 10px 20px -5px rgba(37, 99, 235, 0.3); }

        /* Open Access Batch Cards */
        .batch-card { border-radius: 32px; transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275); background: white; border: 1px solid #e2e8f0; overflow: hidden; }
        .batch-card:hover { transform: translateY(-10px); box-shadow: 0 30px 60px -15px rgba(0, 0, 0, 0.08); border-color: var(--accent); }

        /* Pulse for Live Content */
        .live-tag { background: rgba(239, 68, 68, 0.1); color: #ef4444; border: 1px solid rgba(239, 68, 68, 0.2); }
        .pulse-dot { width: 6px; height: 6px; background: #ef4444; border-radius: 50%; animation: blink 1.5s infinite; }
        @keyframes blink { 0%, 100% { opacity: 1; } 50% { opacity: 0.3; } }
        
        /* Complexity Meter */
        .meter-bar { height: 4px; border-radius: 2px; background: #e2e8f0; flex: 1; }
        .meter-bar.active { background: var(--accent); }
    </style>
</head>
<body class="antialiased">
    <%- include('../partials/header') %>

    <header class="bg-[#020617] pt-40 pb-20 relative overflow-hidden text-center md:text-left">
        <div class="absolute inset-0 opacity-10 bg-[url('https://www.transparenttextures.com/patterns/stardust.png')]"></div>
        <div class="max-w-7xl mx-auto px-6 relative z-10">
            <div class="inline-flex items-center gap-2 px-4 py-2 rounded-full bg-blue-500/10 border border-blue-500/20 mb-8">
                <span class="w-2 h-2 rounded-full bg-blue-500 animate-pulse"></span>
                <span class="text-[10px] font-black text-blue-400 uppercase tracking-[0.2em]">Open Access Initiative 2026</span>
            </div>
            <h1 class="text-5xl md:text-7xl font-black text-white leading-tight mb-6">
                Knowledge is <span class="text-blue-500">Universal.</span>
            </h1>
            <p class="text-slate-400 text-lg md:text-xl max-w-2xl font-medium leading-relaxed">
                Zero-cost, high-fidelity curriculum for the next generation of scientists, from Class 1 foundations to Doctoral research nodes.
            </p>
        </div>
    </header>

    <main class="max-w-7xl mx-auto px-6 py-16">
        <div class="flex flex-col lg:flex-row gap-16">
            
            <aside class="w-full lg:w-72 flex-shrink-0">
                <div class="sticky top-32 space-y-12">
                    <div>
                        <h3 class="text-[10px] font-black text-slate-400 uppercase tracking-widest mb-6">Learning Tiers</h3>
                        <nav class="space-y-1">
                            <% 
                                let strata = [];
                                if (userCountry === 'IN') { strata = ["IIT-JEE", "NEET", "CBSE Boards", "Class 1-10"]; } 
                                else { strata = ["IB Diploma", "SAT / AP", "IGCSE", "Middle School"]; }
                                const finalStrata = [...strata, "Undergraduate", "Post-Grad", "PhD Research"];
                            %>
                            <% finalStrata.forEach(item => { %>
                                <a href="/courses?standard=<%= item %>" class="sidebar-item <%= activeStandard === item ? 'active' : '' %>">
                                    <%= item %>
                                    <svg class="w-3 h-3 opacity-40" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path d="M9 5l7 7-7 7" stroke-width="3"/></svg>
                                </a>
                            <% }) %>
                        </nav>
                    </div>

                    <div>
                        <h3 class="text-[10px] font-black text-slate-400 uppercase tracking-widest mb-6">Research Domains</h3>
                        <nav class="space-y-1">
                            <% ['Mathematics', 'Physics', 'Chemistry', 'Biology', 'Astronomy', 'Computer Science'].forEach(sub => { %>
                                <a href="/courses?subject=<%= sub %>" class="sidebar-item <%= activeSubject === sub ? 'active' : '' %>">
                                    <%= sub %>
                                    <span class="w-1.5 h-1.5 rounded-full bg-slate-200"></span>
                                </a>
                            <% }) %>
                        </nav>
                    </div>
                </div>
            </aside>

            <section class="flex-1">
                <div class="grid grid-cols-1 md:grid-cols-2 gap-10">
                    <% courses.forEach(course => { %>
                        <div class="batch-card group flex flex-col">
                            <div class="h-52 bg-slate-900 relative flex items-center justify-center">
                                <div class="absolute inset-0 opacity-30 bg-gradient-to-tr from-blue-600 to-transparent"></div>
                                <span class="text-7xl z-10 transform group-hover:scale-110 transition-transform duration-500">
                                    <% if(course.subject.includes('Math')) { %>📐<% } 
                                       else if(course.subject.includes('Chem')) { %>🧪<% } 
                                       else if(course.subject.includes('Physics')) { %>⚛️<% } 
                                       else if(course.subject.includes('Astronomy')) { %>🪐<% } 
                                       else { %>💻<% } %>
                                </span>
                                
                                <div class="absolute top-5 left-5">
                                    <span class="px-3 py-1 rounded-lg bg-white/10 backdrop-blur-md text-[9px] font-black text-white uppercase tracking-widest border border-white/10">
                                        <%= course.subject %>
                                    </span>
                                </div>

                                <div class="absolute top-5 right-5">
                                    <div class="live-tag px-3 py-1 rounded-lg flex items-center gap-2 backdrop-blur-sm">
                                        <span class="pulse-dot"></span>
                                        <span class="text-[9px] font-black uppercase">Active Lab</span>
                                    </div>
                                </div>
                            </div>

                            <div class="p-8 flex-1 flex flex-col">
                                <div class="flex items-center gap-3 mb-4">
                                    <span class="text-[10px] font-black text-blue-600 uppercase tracking-tight"><%= course.standard %></span>
                                    <span class="w-1 h-1 bg-slate-200 rounded-full"></span>
                                    <span class="text-[10px] font-bold text-slate-400 uppercase tracking-widest">Mastery Level <%= course.level %></span>
                                </div>

                                <h3 class="text-2xl font-black text-slate-900 leading-tight mb-4 group-hover:text-blue-600 transition-colors">
                                    <%= course.title %>
                                </h3>

                                <p class="text-slate-500 text-sm leading-relaxed mb-8 line-clamp-2">
                                    <%= course.description.replace(/<[^>]*>?/gm, '') %>
                                </p>

                                <div class="mb-8 p-4 bg-slate-50 rounded-2xl border border-slate-100">
                                    <div class="flex justify-between items-center mb-2">
                                        <span class="text-[9px] font-black text-slate-400 uppercase">Module Depth</span>
                                        <span class="text-[9px] font-bold text-blue-600">8 Units</span>
                                    </div>
                                    <div class="flex gap-1">
                                        <div class="meter-bar active"></div>
                                        <div class="meter-bar active"></div>
                                        <div class="meter-bar active"></div>
                                        <div class="meter-bar"></div>
                                        <div class="meter-bar"></div>
                                    </div>
                                </div>

                                <div class="mt-auto pt-6 border-t border-slate-50 flex items-center justify-between">
                                    <div class="flex flex-col">
                                        <span class="text-[9px] font-black text-emerald-600 uppercase tracking-[0.1em]">Access Level</span>
                                        <span class="text-sm font-extrabold text-slate-900">PUBLIC DOMAIN</span>
                                    </div>
                                    <a href="/courses/course/<%= course.id %>" class="bg-slate-900 text-white px-8 py-4 rounded-2xl font-black text-[10px] uppercase tracking-[0.2em] hover:bg-blue-600 transition-all shadow-xl hover:shadow-blue-200">
                                        Start Mission
                                    </a>
                                </div>
                            </div>
                        </div>
                    <% }) %>
                </div>
            </section>
        </div>
    </main>

    

    <%- include('../partials/footer') %>
</body>
</html>