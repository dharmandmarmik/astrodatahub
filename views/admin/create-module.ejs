<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create Ultimate Module | AstroDataHub Admin</title>
    
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdn.quilljs.com/1.3.6/quill.snow.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/katex@0.16.0/dist/katex.min.css">
    <script defer src="https://cdn.jsdelivr.net/npm/katex@0.16.0/dist/katex.min.js"></script>

    <style>
        :root { --accent: #3b82f6; --bg-dark: #0a0a0c; }
        body { background-color: var(--bg-dark); color: #e2e8f0; font-family: 'Inter', sans-serif; }
        
        .ql-toolbar.ql-snow { border-color: #334155; background: #1e293b; border-top-left-radius: 12px; border-top-right-radius: 12px; }
        .ql-container.ql-snow { border-color: #334155; height: 400px; background: #0f172a; border-bottom-left-radius: 12px; border-bottom-right-radius: 12px; font-size: 16px; }
        .ql-editor.ql-blank::before { color: #64748b; }
        
        .form-input { background: #1e293b; border: 1px solid #334155; border-radius: 12px; padding: 12px 16px; color: white; outline: none; transition: 0.2s; }
        .form-input:focus { border-color: var(--accent); box-shadow: 0 0 0 2px rgba(59, 130, 246, 0.2); }
        
        .btn-glow { background: var(--accent); transition: 0.3s; box-shadow: 0 0 15px rgba(59, 130, 246, 0.3); }
        .btn-glow:hover { box-shadow: 0 0 25px rgba(59, 130, 246, 0.5); transform: translateY(-1px); }
    </style>
</head>
<body class="antialiased">
    <%- include('../partials/header') %>

    <main class="max-w-5xl mx-auto px-6 py-24">
        <div class="flex items-center justify-between mb-12">
            <div>
                <h1 class="text-4xl font-black text-white tracking-tight text-uppercase">ADD MISSION MODULE</h1>
                <p class="text-blue-400 font-mono text-sm mt-2 uppercase tracking-tighter">Integration Mode: Active Course #<%= courseId %></p>
            </div>
            <a href="/admin/course/<%= courseId %>" class="text-slate-400 hover:text-white font-bold transition">← Back to Course</a>
        </div>

        <form action="/admin/create-module/<%= courseId %>" method="POST" id="ultimateForm" class="space-y-8">
            
            <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
                <div class="md:col-span-2 flex flex-col">
                    <label class="text-xs font-bold text-slate-500 uppercase mb-2 ml-1">Module Title</label>
                    <input type="text" name="title" placeholder="e.g., Understanding Schwarzschild Radius" required class="form-input">
                </div>
                <div class="flex flex-col">
                    <label class="text-xs font-bold text-slate-500 uppercase mb-2 ml-1">Sequence Order</label>
                    <input type="number" name="module_number" value="1" required class="form-input">
                </div>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                <div class="flex flex-col">
                    <label class="text-xs font-bold text-slate-500 uppercase mb-2 ml-1">Video Integration (Optional)</label>
                    <input type="url" name="video_url" placeholder="https://youtube.com/..." class="form-input">
                </div>
                <div class="flex flex-col">
                    <label class="text-xs font-bold text-slate-500 uppercase mb-2 ml-1">Resources (JSON/Links)</label>
                    <input type="text" name="resources_json" placeholder='{"PDF": "link.com"}' class="form-input font-mono text-xs">
                </div>
            </div>

            <div class="flex flex-col">
                <label class="text-xs font-bold text-slate-500 uppercase mb-2 ml-1">Lesson Content (LaTeX & Media Support)</label>
                <div id="editor"></div>
                <input type="hidden" name="description" id="contentInput">
            </div>

            <div class="pt-6">
                <button type="submit" class="w-full md:w-auto btn-glow bg-blue-600 text-white px-12 py-4 rounded-2xl font-black uppercase tracking-widest text-sm">
                    Deploy to Course Registry
                </button>
            </div>
        </form>
    </main>

    <script src="https://cdn.quilljs.com/1.3.6/quill.js"></script>
    <script>
        var quill = new Quill('#editor', {
            theme: 'snow',
            placeholder: 'Begin your instruction here. Use the Σ button for LaTeX math...',
            modules: {
                toolbar: [
                    [{ 'header': [1, 2, 3, false] }],
                    ['bold', 'italic', 'underline', 'strike'],
                    ['blockquote', 'code-block', 'formula'],
                    [{ 'list': 'ordered'}, { 'list': 'bullet' }],
                    [{ 'color': [] }, { 'background': [] }],
                    ['link', 'image', 'video'],
                    ['clean']
                ]
            }
        });

        document.getElementById('ultimateForm').onsubmit = function() {
            var html = quill.root.innerHTML;
            document.getElementById('contentInput').value = html;
            return true;
        };
    </script>
</body>
</html>