<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AstroDataHub | Universal Education Hub</title>
    <link rel="stylesheet" href="/css/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;600;700;800&display=swap" rel="stylesheet">
    <style>
        :root { 
            --accent: #2563eb; 
            --accent-dark: #1d4ed8;
            --text-dark: #0f172a; 
            --text-muted: #64748b;
            --bg-light: #f8fafc;
            --border: #e2e8f0;
        }
        
        body { 
            margin: 0; 
            font-family: 'Plus Jakarta Sans', sans-serif; 
            background: #ffffff; 
            color: var(--text-dark); 
            scroll-behavior: smooth; 
        }

        /* --- Hero Section --- */
        .hero { 
            min-height: 85vh; 
            display: flex; 
            flex-direction: column; 
            justify-content: center; 
            align-items: center; 
            text-align: center; 
            background: radial-gradient(circle at 70% 30%, #eff6ff 0%, #ffffff 70%); 
            padding: 0 10%; 
        }
        .hero h1 { font-size: clamp(2.5rem, 8vw, 4.2rem); line-height: 1.1; margin: 0; color: var(--text-dark); font-weight: 800; letter-spacing: -2px; }
        .hero p { max-width: 800px; font-size: 1.25rem; color: var(--text-muted); margin: 30px 0; line-height: 1.6; }

        /* --- Perfectly Centered Button Design --- */
        .btn { 
            height: 56px;
            padding: 0 40px; 
            border-radius: 50px; 
            font-weight: 700; 
            text-decoration: none; 
            display: inline-flex; 
            align-items: center; 
            justify-content: center; 
            text-align: center;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1); 
            font-size: 1rem; 
            border: none;
            cursor: pointer;
            letter-spacing: 0.5px;
            white-space: nowrap;
        }

        .btn-primary { 
            background: linear-gradient(135deg, var(--accent) 0%, var(--accent-dark) 100%); 
            color: #fff; 
            box-shadow: 0 4px 15px rgba(37, 99, 235, 0.3);
        }

        .btn-primary:hover { 
            transform: translateY(-2px); 
            box-shadow: 0 8px 25px rgba(37, 99, 235, 0.4);
            background: linear-gradient(135deg, #3b82f6 0%, var(--accent) 100%);
        }

        .btn-outline {
            background: transparent; 
            color: var(--text-dark); 
            border: 2px solid var(--border);
            height: 52px; 
        }

        .btn-white { 
            background: #ffffff; 
            color: var(--accent); 
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.05);
        }

        /* --- Stats Bar --- */
        .stats-bar {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            background: #ffffff;
            border: 1px solid var(--border);
            border-radius: 20px;
            padding: 30px;
            max-width: 1000px;
            margin: -60px auto 0;
            box-shadow: 0 20px 50px rgba(0,0,0,0.05);
            text-align: center;
        }
        .stat-item h2 { color: var(--accent); margin: 0; font-size: 2rem; font-weight: 800; }
        .stat-item p { color: var(--text-muted); margin: 5px 0 0; font-weight: 600; font-size: 0.8rem; text-transform: uppercase; letter-spacing: 1px; }

        /* --- Sections --- */
        .section { padding: 100px 10%; border-top: 1px solid var(--border); }
        .section-title { font-size: 2.5rem; text-align: center; margin-bottom: 20px; font-weight: 800; letter-spacing: -1px; }
        .section-subtitle { text-align: center; color: var(--text-muted); margin-bottom: 60px; font-size: 1.1rem; max-width: 700px; margin-left: auto; margin-right: auto; }
        
        /* --- Learning Path Grid --- */
        .path-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(320px, 1fr)); gap: 25px; }
        .path-card { 
            background: #ffffff; 
            padding: 35px; 
            border-radius: 24px; 
            border: 1px solid var(--border); 
            transition: all 0.3s ease;
            display: flex;
            flex-direction: column;
        }
        .path-card:hover { border-color: var(--accent); transform: translateY(-5px); box-shadow: 0 15px 35px rgba(0,0,0,0.05); }
        .level-tag { font-size: 0.75rem; font-weight: 800; text-transform: uppercase; color: var(--accent); margin-bottom: 10px; letter-spacing: 1px; }
        .path-card h3 { margin: 0 0 15px 0; font-size: 1.4rem; font-weight: 700; }
        .path-card p { color: var(--text-muted); font-size: 0.95rem; line-height: 1.6; margin-bottom: 20px; }
        .path-card ul { padding: 0; list-style: none; margin-top: auto; }
        .path-card li { font-size: 0.85rem; padding: 8px 0; border-top: 1px solid #f1f5f9; color: var(--text-dark); font-weight: 600; display: flex; align-items: center; gap: 8px; }

        /* --- Call to Action --- */
        .cta-section { 
            background: var(--text-dark); 
            color: white; 
            text-align: center; 
            padding: 80px 10%; 
            border-radius: 40px; 
            margin: 0 5% 80px;
        }
        .cta-section h2 { font-size: 2.5rem; margin-bottom: 20px; font-weight: 800; }
        .cta-section p { color: #94a3b8; margin-bottom: 40px; max-width: 600px; margin: 0 auto 40px; font-size: 1.1rem; }

        /* --- Footer --- */
        footer { padding: 80px 10%; border-top: 1px solid var(--border); background: var(--bg-light); text-align: center; }
    </style>
</head>
<body>

    <%- include('partials/header') %>

    <header class="hero">
        <h1>Comprehensive Education <br> for <span style="color:var(--accent)">School, College & Beyond.</span></h1>
        <p>A full-scale educational ecosystem. We provide specialized coaching for school studies, high-school sciences, and university-level research, all integrated with the wonders of Astronomy and Mathematics.</p>
        <div style="display: flex; gap: 20px; margin-top: 10px;">
            <a href="/auth/register" class="btn btn-primary">Join the Hub</a>
            <a href="#paths" class="btn btn-outline">Explore Courses</a>
        </div>
    </header>

    <div class="stats-bar">
        <div class="stat-item">
            <h2>Nursery-PG</h2>
            <p>Academic Range</p>
        </div>
        <div class="stat-item">
            <h2>School+</h2>
            <p>Study Curriculum</p>
        </div>
        <div class="stat-item">
            <h2>Expert</h2>
            <p>Faculty Support</p>
        </div>
        <div class="stat-item">
            <h2>12k+</h2>
            <p>Active Students</p>
        </div>
    </div>

    <section id="about" class="section">
        <h2 class="section-title">Our Guiding Principles</h2>
        <p class="section-subtitle">Blending standard academic excellence with deep scientific curiosity.</p>
        <div class="path-grid" style="grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));">
            <div class="path-card">
                <h3 style="color: var(--accent);">Data-Driven Research</h3>
                <p>We support rigorous research, connecting theoretical concepts with practical applications in space science.</p>
            </div>
            <div class="path-card">
                <h3 style="color: var(--accent);">Stay Updated</h3>
                <p>Our community actively tracks and discusses the latest scientific discoveries and technological advancements.</p>
            </div>
            <div class="path-card">
                <h3 style="color: var(--accent);">Foster Curiosity</h3>
                <p>We encourage intellectual growth by sharing diverse perspectives where curiosity is nurtured.</p>
            </div>
        </div>
    </section>

    <section id="paths" class="section" style="background: var(--bg-light);">
        <h2 class="section-title">Academic Learning Paths</h2>
        <p class="section-subtitle">Structured courses covering school board syllabus and professional research levels.</p>
        
        <div class="path-grid">
            <div class="path-card">
                <span class="level-tag">Early Childhood</span>
                <h3>Nursery & Pre-School</h3>
                <p>Foundational learning for toddlers. Focus on shapes, numbers, alphabets, and identifying the Moon and Stars.</p>
                <ul>
                    <li>🎨 Creative Arts & Colors</li>
                    <li>🔢 Introduction to Numbers</li>
                    <li>🌙 Night Sky Storytelling</li>
                </ul>
            </div>

            <div class="path-card">
                <span class="level-tag">Primary & Secondary</span>
                <h3>High School & Boards</h3>
                <p>Complete support for school subjects (Science, Math, etc.) following standard board curriculums.</p>
                <ul>
                    <li>📚 School Syllabus Support</li>
                    <li>🔬 Biology, Chemistry & Physics</li>
                    <li>📐 Arithmetic & Algebra</li>
                </ul>
            </div>

            <div class="path-card">
                <span class="level-tag">University & Research</span>
                <h3>College & Post-Grad</h3>
                <p>Specialized courses for degrees and professional research in Theoretical Physics and Data Science.</p>
                <ul>
                    <li>⚛️ Advanced Quantum Physics</li>
                    <li>📈 Complex Calculus</li>
                    <li>📊 Research Data Analysis</li>
                </ul>
            </div>
        </div>
    </section>

    <section id="domains" class="section">
        <h2 class="section-title">Interdisciplinary Domains</h2>
        <div class="path-grid" style="grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));">
            <div style="border-left: 4px solid var(--accent); padding-left: 20px;">
                <h4 style="margin-bottom:10px;">Astronomy & Cosmology</h4>
                <p style="font-size:0.9rem; color:var(--text-muted);">From basic planet spotting to Galaxy formation and Black Holes.</p>
            </div>
            <div style="border-left: 4px solid var(--accent); padding-left: 20px;">
                <h4>Standard Sciences</h4>
                <p style="font-size:0.9rem; color:var(--text-muted);">Comprehensive school-level Physics, Chemistry, and Biology.</p>
            </div>
            <div style="border-left: 4px solid var(--accent); padding-left: 20px;">
                <h4>Academic Mathematics</h4>
                <p style="font-size:0.9rem; color:var(--text-muted);">K-12 Mathematics, Geometry, Calculus, and Topology.</p>
            </div>
            <div style="border-left: 4px solid var(--accent); padding-left: 20px;">
                <h4>Computer Sciences</h4>
                <p style="font-size:0.9rem; color:var(--text-muted);">Programming for kids, Big Data handling, and AI for research.</p>
            </div>
        </div>
    </section>

    <div class="cta-section">
        <h2>Ready to Start Your Education?</h2>
        <p>AstroDataHub provides the perfect balance of school study and scientific exploration. Join our hub for students of all ages.</p>
        <a href="/auth/register" class="btn btn-white">Create Free Account</a>
    </div>

    <footer>
        <h2 style="font-weight: 800; margin: 0;"><span style="color:var(--accent)">ASTRO</span>DATAHUB</h2>
        <p style="color: var(--text-muted); max-width: 500px; margin: 20px auto;">
            Empowering students from nursery and primary school through to professional university research.
        </p>
        <nav style="margin: 30px 0;">
            <ul style="list-style: none; padding: 0; display: flex; gap: 30px; justify-content: center;">
                <li><a href="/courses" style="text-decoration: none; color: var(--text-dark); font-weight: 600; font-size: 0.9rem;">Catalog</a></li>
                <li><a href="/faq" style="text-decoration: none; color: var(--text-dark); font-weight: 600; font-size: 0.9rem;">FAQ</a></li>
                <li><a href="/support" style="text-decoration: none; color: var(--text-dark); font-weight: 600; font-size: 0.9rem;">Support</a></li>
            </ul>
        </nav>
        <div style="border-top: 1px solid var(--border); padding-top: 30px; width: 100%;">
            <p style="color: var(--text-muted); font-size: 0.85rem;">&copy; 2025 AstroDataHub Sector. All rights reserved.</p>
        </div>
    </footer>

</body>
</html>