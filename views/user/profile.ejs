<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> | AstroDataHub</title>
    <link rel="stylesheet" href="/css/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <script src="https://unpkg.com/lucide@latest"></script>
    <style>
        body { background-color: #f8fafc; color: #1e293b; font-family: 'Inter', sans-serif; margin: 0; }
        
        .profile-header { background: #ffffff; border-bottom: 1px solid #e2e8f0; padding: 4rem 0 3rem; text-align: center; }

        .avatar-main {
            width: 110px; height: 110px; background: linear-gradient(135deg, #2563eb, #7c3aed);
            color: white; border-radius: 28px; display: flex; align-items: center; justify-content: center;
            font-size: 3.8rem; font-weight: 800; margin: 0 auto 1.5rem; box-shadow: 0 12px 30px -5px rgba(37, 99, 235, 0.4);
        }

        .profile-container { max-width: 1100px; margin: -3rem auto 4rem; padding: 0 20px; }

        .stats-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 1.5rem; margin-bottom: 2rem; }
        .stat-card { background: white; padding: 1.75rem; border-radius: 20px; border: 1px solid #e2e8f0; text-align: center; box-shadow: 0 4px 6px -1px rgba(0,0,0,0.05); }
        .stat-value { font-size: 1.8rem; font-weight: 800; color: #0f172a; display: block; margin-bottom: 4px; }
        .stat-label { font-size: 0.7rem; color: #64748b; text-transform: uppercase; letter-spacing: 0.12em; font-weight: 700; }

        .badge-container { background: white; border-radius: 28px; border: 1px solid #e2e8f0; padding: 3rem; box-shadow: 0 20px 25px -5px rgba(0,0,0,0.05); }
        .badge-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(180px, 1fr)); gap: 1.5rem; margin-top: 2.5rem; }

        .badge-item {
            background: #ffffff; border: 1px solid #f1f5f9; padding: 2rem 1rem; border-radius: 24px;
            text-align: center; transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275); position: relative;
        }
        .badge-item:hover { transform: translateY(-12px); border-color: #3b82f6; box-shadow: 0 20px 25px -5px rgba(0,0,0,0.08); }

        .badge-hex {
            width: 72px; height: 72px; margin: 0 auto 1.25rem; display: flex; align-items: center; justify-content: center;
            border-radius: 22px; color: white;
        }

        /* Subject Gradients */
        .bg-physics { background: linear-gradient(135deg, #3b82f6, #1d4ed8); }
        .bg-astronomy { background: linear-gradient(135deg, #8b5cf6, #5b21b6); }
        .bg-chemistry { background: linear-gradient(135deg, #10b981, #047857); }
        .bg-biology { background: linear-gradient(135deg, #ec4899, #be185d); }
        .bg-math { background: linear-gradient(135deg, #f59e0b, #b45309); }
        .bg-engineering { background: linear-gradient(135deg, #64748b, #334155); }
        .bg-default { background: linear-gradient(135deg, #94a3b8, #475569); }

        .badge-name { font-weight: 800; font-size: 0.95rem; color: #0f172a; display: block; line-height: 1.3; margin-bottom: 6px; }
        .badge-subject { font-size: 0.65rem; color: #3b82f6; font-weight: 800; text-transform: uppercase; letter-spacing: 0.08em; }

        .xp-progress { background: #f1f5f9; height: 16px; border-radius: 20px; margin-top: 15px; overflow: hidden; border: 1px solid #e2e8f0; }
        .xp-bar { height: 100%; background: linear-gradient(90deg, #2563eb, #9333ea); transition: width 1s ease-in-out; }

        .locked-badge { opacity: 0.45; filter: grayscale(1); border: 2px dashed #e2e8f0; background: #f8fafc; }
        .join-date-tag { display: inline-flex; align-items: center; gap: 6px; background: #f1f5f9; padding: 6px 12px; border-radius: 8px; font-size: 0.8rem; font-weight: 600; color: #64748b; margin-top: 10px; }

        @media (max-width: 640px) { .stats-grid { grid-template-columns: 1fr; } .badge-grid { grid-template-columns: repeat(2, 1fr); } }
    </style>
</head>
<body>

    <%- include('../partials/header') %>

    <header class="profile-header">
        <div class="avatar-main">
            <%= profileUser.username.charAt(0).toUpperCase() %>
        </div>
        <h1 style="margin: 0; font-size: 2.75rem; font-weight: 800; letter-spacing: -0.03em;"><%= profileUser.username %></h1>
        <p style="color: #475569; margin: 0.5rem 0 0; font-weight: 700; font-size: 1.1rem;">Level <%= profileUser.level %> Explorer</p>
        
        <div class="join-date-tag">
            <i data-lucide="calendar" style="width: 14px; height: 14px;"></i>
            Enlisted <%= new Date(profileUser.created_at).toLocaleDateString('en-US', { month: 'long', day: 'numeric', year: 'numeric' }) %>
        </div>

        <div style="max-width: 380px; margin: 2rem auto 0;">
            <div class="flex justify-between items-center px-1">
                <span style="font-size: 0.7rem; font-weight: 800; color: #94a3b8; text-transform: uppercase;">Rank Progress</span>
                <span style="font-size: 0.7rem; font-weight: 800; color: #2563eb; text-transform: uppercase;"><%= profileUser.xp %> XP</span>
            </div>
            <div class="xp-progress">
                <div class="xp-bar" style="width: <%= (profileUser.xp % 1000) / 10 %>%;"></div>
            </div>
        </div>
    </header>

    <main class="profile-container">
        
        <div class="stats-grid">
            <div class="stat-card">
                <span class="stat-value"><%= profileUser.level %></span>
                <span class="stat-label">Mission Rank</span>
            </div>
            <div class="stat-card">
                <span class="stat-value text-blue-600"><%= badges.length %></span>
                <span class="stat-label">Mastery Badges</span>
            </div>
            <div class="stat-card">
                <span class="stat-value">
                    <%= new Date(profileUser.created_at).toLocaleDateString('en-US', { month: 'short', year: 'numeric' }) %>
                </span>
                <span class="stat-label">Fleet Enlistment</span>
            </div>
        </div>

        <div class="badge-container">
            <div style="display: flex; justify-content: space-between; align-items: center; border-bottom: 2px solid #f1f5f9; padding-bottom: 2rem;">
                <div>
                    <h2 style="margin: 0; font-size: 1.6rem; font-weight: 800; color: #0f172a;">🏅 Scientific Credentials</h2>
                    <p style="margin: 6px 0 0; font-size: 0.9rem; color: #64748b;">Complete missions to unlock all 50 verified mastery badges.</p>
                </div>
                <div style="text-align: right;">
                    <span style="display: block; font-size: 1.5rem; font-weight: 900; color: #2563eb;"><%= Math.round((badges.length / 50) * 100) %>%</span>
                    <span style="font-size: 0.65rem; font-weight: 800; color: #94a3b8; text-transform: uppercase; letter-spacing: 0.05em;">Completion</span>
                </div>
            </div>
            
            <div class="badge-grid">
                <% 
                    const getBadgeConfig = (subject) => {
                        const sub = (subject || 'default').toLowerCase();
                        if (sub.includes('physic')) return { class: 'bg-physics', icon: 'atom' };
                        if (sub.includes('astro')) return { class: 'bg-astronomy', icon: 'orbit' };
                        if (sub.includes('chem')) return { class: 'bg-chemistry', icon: 'flask-conical' };
                        if (sub.includes('biol')) return { class: 'bg-biology', icon: 'dna' };
                        if (sub.includes('math')) return { class: 'bg-math', icon: 'calculator' };
                        if (sub.includes('engine')) return { class: 'bg-engineering', icon: 'settings' };
                        return { class: 'bg-default', icon: 'award' };
                    };
                %>

                <% if (badges && badges.length > 0) { %>
                    <% badges.forEach(badge => { 
                        const config = getBadgeConfig(badge.subject);
                    %>
                        <div class="badge-item">
                            <div class="badge-hex <%= config.class %>">
                                <i data-lucide="<%= config.icon %>" style="width: 36px; height: 36px;"></i>
                            </div>
                            <span class="badge-name"><%= badge.title %></span>
                            <span class="badge-subject"><%= badge.subject %></span>
                        </div>
                    <% }) %>
                <% } %>

                <% for(let i = 0; i < (50 - (badges ? badges.length : 0)); i++) { %>
                    <div class="badge-item locked-badge">
                        <div class="badge-hex bg-slate-200">
                            <i data-lucide="lock" style="width: 28px; height: 28px; color: #94a3b8;"></i>
                        </div>
                        <span class="badge-name" style="color: #94a3b8;">Classified</span>
                        <span class="badge-subject" style="color: #cbd5e1;">Mission Pending</span>
                    </div>
                <% } %>
            </div>
        </div>

    </main>

    <%- include('../partials/header') %>

    <script>
        lucide.createIcons();
    </script>

</body>
</html>